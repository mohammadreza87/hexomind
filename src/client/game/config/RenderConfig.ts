/**
 * Rendering configuration for the game
 * Uses pure Phaser graphics for all rendering
 */
export class RenderConfig {
  // Always use programmatic rendering
  static readonly USE_PNG_HEXAGONS = false;
}